table:
  name: stars
  schema: public
array_relationships:
  - name: movie_stars
    using:
      foreign_key_constraint_on:
        column: star_id
        table:
          name: movie_stars
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - name
        - bio
        - photo_url
        - created_at
        - update_at
        - id
      filter: {}
      allow_aggregations: true
    comment: ""
