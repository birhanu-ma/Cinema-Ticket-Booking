table:
  name: directors
  schema: public
array_relationships:
  - name: movies
    using:
      foreign_key_constraint_on:
        column: director_id
        table:
          name: movies
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - name
        - bio
        - photo_url
        - created_at
        - update_at
        - id
      filter: {}
      allow_aggregations: true
    comment: ""
